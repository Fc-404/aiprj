# aiprj.code.md — AI 工程文件体系 · 代码项目专用

> 本文档是 `aiprj/` 目录的使用指南，专门用于**代码编辑项目**。
> 后缀 `.code.md` 标识这是一份面向代码开发的工程文件模板。

`aiprj/` 是项目根目录下的一套 markdown 工程文档，**写给 AI 看**，让 AI 快速理解、定位、开展代码编辑工作。

---

## 核心规则

### 📄 文档规范

| # | 规则 |
|---|------|
| 1 | **格式统一** — 所有文件用 markdown，UTF-8 编码，末尾标注版本号和日期 |
| 2 | **精简优先** — 多用表格/代码块/列表，避免长篇大论 |
| 3 | **按需创建，建则维护** — README、ARCHITECTURE、DIRECTORY、CHANGELOG 为必建文件，其余按项目情况可选；建了的文件就要持续维护 |

### 🔄 变更管理

| # | 规则 |
|---|------|
| 4 | **先读日志** — 每轮对话开始先读 CHANGELOG，确认是否有新修正 |
| 5 | **纠正即记** — 用户每次纠正必须记入 CHANGELOG，同步更新受影响文件 |
| 6 | **变更透明** — 代码或文档变更后，告知用户更新了哪些文件 |

### 🤝 协作意识

| # | 规则 |
|---|------|
| 7 | **先问后动** — 新增/删除/重命名文件或调整结构前，先征求用户同意 |
| 8 | **Git 历史管理** — 用本地 Git 记录代码变更历史，方便回溯和回退；commit 前 `git status` 确认变更范围，提交信息简明描述即可 |

---

## 文件清单

```
aiprj/
├── 00-PLAN.md              [推荐] 项目计划：任务分解、逻辑依赖、进度追踪 ⭐
├── README.md               [必建] 项目入口 + 约束文件：背景、目标、核心原则、文件索引
├── 01-ARCHITECTURE.md      [必建] 架构：模块划分/部署拓扑/数据流
├── 02-TECH-STACK.md        [可选] 技术栈：语言/框架/依赖/版本
├── 03-API-DESIGN.md        [可选] API：接口定义/请求响应/错误码
├── 04-DATABASE.md          [可选] 数据库：表结构/字段说明/查询
├── 05-PROTOCOL.md          [可选] 协议：通信协议/设备协议/特殊约定
├── 06-DIRECTORY.md         [必建] 目录：代码结构/文件职责/命名规范
├── 07-DEVELOPMENT.md       [可选] 开发：环境搭建/编译运行/部署
├── 08-REFERENCES.md        [可选] 参考：术语表/外部链接/FAQ
└── 09-CHANGELOG.md         [必建] 修正：用户纠正/架构调整/决策 ⭐
```

**必建文件（4 个）：** 任何项目都需要的骨架。
**推荐文件（1 个）：** 00-PLAN.md 建议创建，让 AI 和用户对项目范围、任务节奏与优先级达成一致。
**可选文件（6 个）：** 取决于项目规模和特性，AI 判断是否需要。

---

## AI 创建流程

### 第一步：了解项目

不要一次性抛出固定问题列表。根据用户已经提供的信息，**逐步推理出还缺什么**，再有针对性地追问。

**追问技巧：**
- 用户说了什么，你就基于它继续深挖
- 用户没说到的，根据常见项目结构**推理缺失的信息**，然后询问
- 如果用户描述模糊，可以给出选项让用户选（比如"数据库用 SQLite 还是 MySQL？"）

### 第二步：创建文件

根据收集到的信息创建 `aiprj/` 目录及对应文件：

- **必建 4 个文件** 无论如何都要创建
- **推荐创建 00-PLAN.md** 项目计划文件，明确任务分解和逻辑依赖
- **可选文件** 按需创建。例如：
  - 没有数据库 → 不建 `04-DATABASE.md`
  - 没有 API → 不建 `03-API-DESIGN.md`
  - 没有特殊协议 → 不建 `05-PROTOCOL.md`
  - 技术栈很常规 → `02-TECH-STACK.md` 可以简略或省略
  - 开发流程很简单 → `07-DEVELOPMENT.md` 可以省略

### 第三步：持续维护

按**核心规则**执行，聚焦以下环节：

| 时机 | 要做的事 | 对应规则 |
|------|---------|---------|
| 每轮对话开始 | 先读 CHANGELOG，看有没有新修正 | 规则 4 |
| 代码/文档变更后 | 同步更新对应文件 + 告知用户改了什么 | 规则 5、6 |
| 想调整文件结构 | 先问用户，等确认了再动手 | 规则 7 |
| 每次代码变更 | `git add` + `git commit` 记录，方便回溯 | 规则 8 |

---

## 各文件说明

### 00-PLAN.md [推荐]
项目的**作战地图**。明确项目范围和任务分工，让 AI 清楚先做什么、后做什么。
- 适合放：项目目标概述、任务分解（WBS）、**逻辑依赖关系（DAG）**、当前进度追踪
- **不需要时间节点** — AI 完成任务不依赖时间评估，只需明确任务间的先后依赖
- 依赖标注：`T1 → T2` 表示 T1 完成后才能做 T2，无依赖的任务可**并行**
- 进度更新：**每轮对话**同步更新状态，已完成任务打标，新任务及时追加
- 任务状态：✅ 完成 / 🔄 进行中 / ⏳ 待开始 / ⛔ 阻塞 / ⬜ 未规划
- 计划调整：范围或优先级变更时，更新 PLAN.md 并同步到 CHANGELOG

### README.md
项目的**入口 + 约束 + 索引**三合一。告诉 AI 项目的背景目标、关键约束原则，同时作为其他文件的导航索引。
- 核心设计原则一定要写清楚，这些是约束 AI 行为的规则
- 文件导航表让 AI 知道每个文件是做什么的
- 适合放：项目名及简单描述、核心设计原则、工程文件导航表、相关引用链接

### 01-ARCHITECTURE.md
项目的骨架。AI 读完知道模块怎么划分、部署在哪、怎么通信。
- 适合放：架构图（ASCII）、模块职责表、部署拓扑、网络/IP/端口规划、关键数据流

### 02-TECH-STACK.md [可选]
有特定技术选型时创建。列出语言、框架、依赖库及版本。
- 适合放：后端/前端/工具链各自的技术栈表、关键依赖及用途

### 03-API-DESIGN.md [可选]
项目有对外提供 API 时创建。前后端或跨服务对接的契约。
- 适合放：Base URL、通用约定、各接口路径/方法/参数/响应示例、WS 消息、错误码

### 04-DATABASE.md [可选]
项目使用数据库时创建。数据层的完整描述。
- 适合放：表结构及字段含义、数据类型、索引、常用 SQL、代码模型

### 05-PROTOCOL.md [可选]
项目有非标准协议时创建。通用的 REST/WS API 不放在这里。
- 适合放：协议帧格式、编解码算法、校验算法、设备地址映射、寄存器定义

### 06-DIRECTORY.md
代码的地图。AI 知道改某个功能要去哪个文件。
- 适合放：目录树（标注关键目录）、每文件职责表、命名规范、关键路径速查

### 07-DEVELOPMENT.md [可选]
需要说明环境搭建或构建步骤时创建。
- 适合放：环境要求、安装命令、开发/构建/部署命令、**Git 工作流策略（分支策略、提交规范、回退操作指引）**、FAQ

### 08-REFERENCES.md [可选]
需要术语表或外部参考时创建。
- 适合放：术语中英对照、参考项目索引、外部链接、常见问题

### 09-CHANGELOG.md ⭐
**最重要，必须维护。** 记录所有纠正和关键决策。
- 每条记录包含：日期、类型、提议人、问题背景、纠正/决策内容、影响文件、原因
- 最新记录放最上面
- 类型：用户纠正 / 架构调整 / 决策记录 / 踩坑记录

---

*本文档版本：v3.2 | 最后更新：2026-05-15*
*作者：小雨 (xia-yu)*
